# Local deployment config (copy to bro_private/.env; do NOT commit bro_private/.env)
#
# Production SSH
PROD_SSH_HOST=ubuntu@154.8.205.159
PROD_SSH_PORT=22

# Production app/service layout
PROD_APP_DIR=/srv/we-mp-rss/app
PROD_SERVICE=we-mp-rss
DEPLOY_BRANCH=deploy

# Optional: override venv python on server
VENV_PY=/srv/we-mp-rss/venv/bin/python

# Optional: set to 0 to skip pip install during deploy
RUN_PIP_INSTALL=1
